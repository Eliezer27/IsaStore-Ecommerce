export default function ContactPage() {
  return (
    <div className="container py-5">
      <h1 className="h4 mb-3">Contacto</h1>
      <p className="text-secondary">
        Pendiente: formulario de contacto y mapa de Google Maps (Maps
        JavaScript API) usando la tabla <code>store_locations</code>. Ver la
        sección 6 del documento de arquitectura. Ya está instalado{" "}
        <code>@react-google-maps/api</code>; falta la
        <code> NEXT_PUBLIC_GOOGLE_MAPS_API_KEY</code> en <code>.env.local</code>.
      </p>
    </div>
  );
}
